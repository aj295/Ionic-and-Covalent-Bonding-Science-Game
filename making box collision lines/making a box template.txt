left = left most x coordinate
right = right most x coordinate
top = upper most y coordinate
bottom = lower most y coordinate

let topLine = new Line(100 * left / window_width, 100 * top / window_height, 100 * right / window_width, 100 * top / window_height, thisLevel)
let bottomLine = new Line(100 * left / window_width, 100 * bottom / window_height, 100 * right / window_width, 100 * bottom / window_height, thisLevel)
let leftLine = new Line(100 * left / window_width, 100 * top / window_height, 100 * left / window_width, 100 * bottom / window_height, thisLevel)
let rightLine = new Line(100 * right / window_width, 100 * top / window_height, 100 * right / window_width, 100 * bottom / window_height, thisLevel)

left top right top
left bottom right bottom
left top left bottom
right top right bottom